"""An MCP server exposing tahidromos to AI agents.

Agents that handle email need somewhere to practise. The existing options are
production inbox services: real addresses, real delivery, a bill, and an
internet connection. This gives an agent a disposable mailbox on a mail server
running on your laptop, where nothing can escape and a counterparty that
always answers is one call away.

    tahidromos-mcp

Reads TAHIDROMOS_URL (default http://localhost:8080).
"""

__version__ = "1.0.0"
